# Notas: 

Primer programa en node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -n, --number   Numero base de la tabla de multiplicar     [número] [requerido]
  -l, --list     muestra la tabla en consola         [booleano] [defecto: false]
  -t, --times    Hasta donde queremos que llegue la tabla de multiplicar
                                                            [número] [requerido]
```